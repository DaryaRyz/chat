import 'package:flutter/material.dart';

class ChatsDetails extends StatelessWidget {
  const ChatsDetails({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
